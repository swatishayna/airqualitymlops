from setuptools import setup, find_packages

setup(
    name="src",
    version="0.0.1",
    description="its an air temperature prediction package",
    author="swati",
    packages=find_packages(),
    license="MIT"
)
